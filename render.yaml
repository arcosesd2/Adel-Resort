databases:
  - name: adel-beach-resort-db
    databaseName: adel_beach_resort
    plan: free
    region: singapore

services:
  # Django Backend
  - type: web
    name: adel-beach-resort-backend
    env: python
    region: singapore
    plan: free
    buildCommand: "chmod +x ./backend/build.sh && ./backend/build.sh"
    startCommand: "cd backend && gunicorn hotel.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: hotel.settings
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: adel-beach-resort-db
          property: connectionString
      - key: CORS_ALLOWED_ORIGINS
        sync: false  # Set this to your frontend Render URL after deploying frontend
      - key: CLOUDINARY_CLOUD_NAME
        sync: false  # Set your Cloudinary cloud name
      - key: CLOUDINARY_API_KEY
        sync: false  # Set your Cloudinary API key
      - key: CLOUDINARY_API_SECRET
        sync: false  # Set your Cloudinary API secret
      - key: FRONTEND_URL
        sync: false  # Set to frontend Render URL
      - key: PYTHON_VERSION
        value: "3.12.0"

  # Next.js Frontend
  - type: web
    name: adel-beach-resort-frontend
    env: node
    region: singapore
    plan: free
    buildCommand: "cd frontend && npm install && npm run build"
    startCommand: "cd frontend && npm run start"
    envVars:
      - key: NEXT_PUBLIC_API_URL
        sync: false  # Set to backend Render URL after deploying backend
      - key: NODE_VERSION
        value: "20"
      - key: PORT
        value: "3000"
