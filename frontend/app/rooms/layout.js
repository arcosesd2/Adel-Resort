export const metadata = {
  title: 'Accommodations',
  description: 'Browse cottages, rooms, and event spaces at Adel Beach Resort. Find your perfect beach getaway.',
}

export default function RoomsLayout({ children }) {
  return children
}
