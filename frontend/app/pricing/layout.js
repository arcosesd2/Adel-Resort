export const metadata = {
  title: 'Pricing',
  description: 'View transparent pricing for all accommodations at Adel Beach Resort. Day and night rates available.',
}

export default function PricingLayout({ children }) {
  return children
}
