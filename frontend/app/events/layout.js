export const metadata = {
  title: 'Upcoming Events',
  description: 'Join us for exciting events and activities at Adel Beach Resort in Lawigan, Surigao Del Sur.',
}

export default function EventsLayout({ children }) {
  return children
}
